{
  "name": "ai-providers-server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "bun run src/server.ts",
    "dev": "bun run --watch src/server.ts",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "test:coverage": "bun test --coverage"
  },
  "dependencies": {
    "ai": "^5.0.60",
    "ai-sdk-provider-gemini-cli": "latest",
    "ai-sdk-provider-claude-code": "latest",
    "escape-html": "^1.0.3"
  },
  "devDependencies": {
    "@types/escape-html": "^1.0.4",
    "@types/node": "^24.6.2",
    "bun-types": "latest"
  }
}
