# https://moonrepo.dev/docs/config/workspace
$schema: './cache/schemas/workspace.json'

# extends: './shared/workspace.yml'

projects:
  - 'ai-server'
  - 'central_cloud'
  - 'seed_agent'
  - 'singularity_app'
  - 'templates_data'
  # - 'vendor/codex'  # Codex fork for builtin tool filtering (temporarily disabled for compilation)

  # rust-central - Rust workspace (NIFs + services)
  - 'rust-central'

  # Singularity NIFs (7 total)
  - 'rust-central/architecture_engine'
  - 'rust-central/code_engine'
  - 'rust-central/embedding_engine'
  - 'rust-central/parser_engine'
  - 'rust-central/prompt_engine'
  - 'rust-central/quality_engine'
  - 'rust-central/knowledge_engine'

  # Central Cloud Service (1 total - rest moved to Elixir)
  - 'rust-central/package_intelligence'

# Note: Moon caches task outputs under .moon/cache by default. CI caches this
# directory (see .github/workflows/ci-elixir.yml) to speed up repeated runs.
