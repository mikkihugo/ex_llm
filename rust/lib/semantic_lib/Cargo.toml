[package]
name = "semantic_lib"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Semantic analysis and embedding library"

[dependencies]
anyhow = { workspace = true }
serde = { workspace = true, features = ["derive"] }
