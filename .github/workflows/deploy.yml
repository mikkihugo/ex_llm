name: Deploy to Fly.io

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Install Nix
        uses: cachix/install-nix-action@a809471b5c7c913aa67bec8f459a11a0decc3fce # v31
        with:
          extra_nix_config: |
            experimental-features = nix-command flakes

      - name: Setup flyctl
        uses: superfly/flyctl-actions/setup-flyctl@master

      - name: Build with Nix
        run: |
          nix build .#singularity-integrated --show-trace

      - name: Deploy to Fly.io
        run: |
          flyctl deploy --app singularity --config fly-integrated.toml --nixpacks
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
          # AGE_SECRET_KEY is already in fly.io secrets, no need to pass here
